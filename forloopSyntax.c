#include <stdio.h>

int main(){

    // Syntax
    //  for (initializationStatement ; testExpression; updateStatement){
        // Statements inside the body of for;
    // }


    int a ;

    for ( a =2 ; a < 5 ; ++a){
        printf("%d", a);
    }

    return 0;
}